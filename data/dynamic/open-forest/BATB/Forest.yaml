KeySet:
    forward:    
        KeyKeyboardButton: { code: W }
    backward:
        KeyKeyboardButton: { code: S }
    left:
        KeyKeyboardButton: { code: A }
    right:
        KeyKeyboardButton: { code: D }
    aim:
        KeyPointer:
          axis-x:
            KeyMouseAxis: { axis: X }
          axis-y:
            KeyMouseAxis: { axis: Y }
          left:
            KeyMouseButton: { code: Y }
          right:
            KeyMouseButton: { code: T }
    mapview:
        KeyKeyboardButton: { code: L }
    mapview-plus:
        KeyKeyboardButton: { code: K }
    mapview-shift:
        KeyKeyboardButton: { code: SHIFT }
    mapview-ctrl:
        KeyKeyboardButton: { code: CTRL }
    mapview-rotate:
        KeyMouseScroll: { axis: Y, sensitivity: 2.5 }
